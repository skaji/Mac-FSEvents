prereqs:
  configure:
    requires:
      Dist::Build: { version: '0.015' }
  runtime:
    requires:
      perl: { version: 'v5.10.1' }
  test:
    requires:
      Test::More: { version: '0.99' }
  develop:
    requires:
      Test::LeakTrace:
